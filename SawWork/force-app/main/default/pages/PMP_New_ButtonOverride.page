<!--Author: Ruchika Sharma
    Version: 2.0
    SFDC-6322: PMP-PMA flow-->
<apex:page showHeader="true" sidebar="true" extensions="L2Q_PMPFlowController" standardController="Partner_Marketing_Plan__c">
    <apex:includeLightning />
    <div id="lightning" />
    <!-- <apex:pageMessages id="msgId"/> ?flowLayout=twoColumn -->
    <script>
    var currentUrl = window.location.href;
    var accId = '{!internalUserAccId}';
    if({!isPartnerUser}) {
    
            window.location = '{!partnerUserURL}';//'/partners/flow/PMP_PMA_Creation_and_Edit?linkNavigatorPrefix=/partners/&retURL=/partners/a1x/o'; //
        } else if (accId != 'empty') {
            
            window.location = '{!internalUserURL}' + accId;//'/flow/PMP_PMA_Creation_and_Edit?linkNavigatorPrefix=/&retURL=/a1x/o&internalUserSelectedAccount=' + accId; //  
        }
        else {
            var flowUrl = '{!internalUserURL}';//"/flow/PMP_PMA_Creation_and_Edit?linkNavigatorPrefix=/&retURL=/a1x/o&internalUserSelectedAccount=";
            $Lightning.use("c:GSM_GenericFormApplication", function () {
                $Lightning.createComponent("c:L2Q_FlowIntermediateComponent", { titleVar: "Create Partner MDF Plan", lookupFilter: "IsPartnerCustom__c = true", lookupFieldLabel: "Select Partner Account", redirectionUrl: flowUrl }, "lightning", function (cmp) {
                });
            });
        }
    </script>
</apex:page>