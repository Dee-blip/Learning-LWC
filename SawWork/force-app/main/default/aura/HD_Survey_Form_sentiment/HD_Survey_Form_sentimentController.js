({
	facesInitAction : function(cmp, event, helper) {
		helper.faceSetHelper(cmp, event);
        
	},
    setFace: function(cmp, event, helper){
     helper.faceSetHelper(cmp, event);
       
    }
})