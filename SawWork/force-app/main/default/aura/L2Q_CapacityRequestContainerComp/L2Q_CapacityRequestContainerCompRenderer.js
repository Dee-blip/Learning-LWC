({
	afterRender: function(cmp, evt) {
		// //var self = $A.get('e.force:closeQuickAction').fire();
		// setTimeout(function() {
		// 	var quickActionClose = $A.get('e.force:closeQuickAction');
		// 	quickActionClose.fire();
		// }, 4000);
	}

	// Your renderer method overrides go here
});